/*
public class XMLParse {
    public String encoding = "US-ASCII";
    
    public XMLElement parse(InputStream is) throws IOException {
	readElement(is);
    }
    
    public void readXML(PushbackInputStream is) {
	
// 	LinkedList<int> buf = new LinkedList<int>();
// 	int currentByte = is.read();
// 	if(currentByte != '<')
// 	    throw new RuntimeException();
	
// 	while(currentByte != '>' && currentByte != -1) {
// 	    buf.add(currentByte);
// 	    currentByte = is.read();
// 	}
// 	if(currentByte == -1)
// 	    throw new RuntimeException();
// 	else {
// 	    buf.add(currentByte);
// 	    byte[] bytes = new byte[buf.size()];
// 	    int i = 0;
// 	    for(int cur : buf)
// 		bytes[i++] = cur;
// 	    String s = new String(bytes, encoding);
// 	}
    }
}
*/
